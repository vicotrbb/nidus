[package]
name = "nidus-example-integrations-production"
version = "1.0.9"
edition = "2024"
publish = false

[dependencies]
nidus = { package = "nidus-rs", path = "../../crates/nidus", version = "1.0.9", features = ["http", "config", "observability"] }
nidus-cache = { path = "../../crates/nidus-cache", version = "1.0.9", features = ["health", "observability"] }
nidus-sqlx = { path = "../../crates/nidus-sqlx", version = "1.0.9", features = ["sqlite", "health", "observability"] }
tokio.workspace = true
