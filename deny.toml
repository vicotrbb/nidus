[advisories]
version = 2
yanked = "warn"
ignore = [
  { id = "RUSTSEC-2026-0173", reason = "validator 0.20 derives currently depend on proc-macro-error2 and no safe fixed upgrade is available; revisit when validator migrates derive diagnostics." },
]

[licenses]
version = 2
allow = ["Apache-2.0", "MIT", "Unicode-3.0", "BSD-3-Clause"]

[bans]
multiple-versions = "allow"
wildcards = "deny"
